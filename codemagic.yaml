workflows:
  react-native-ios:
    name: AllergyBuster iOS
    max_build_duration: 60
    instance_type: mac_mini_m2

    environment:
      groups:
        - app_store
      ios_signing:
        distribution_type: app_store
        bundle_identifier: com.allergybusterapp
      vars:
        BUNDLE_ID: "com.allergybusterapp"
      node: 22.11.0

    scripts:
      - name: Install dependencies
        script: |
          cd AllergyBusterApp
          npm ci

      - name: Install CocoaPods
        script: |
          # Pin openiap apple pod to 2.2.5 (Xcode 26.4 final SDK compatible).
          # openiap 2.2.1 (bundled with react-native-iap@15.3.2) used iOS 26 beta
          # StoreKit APIs (.billingPlanType, .pricingTerms, etc.) that were removed
          # from the Xcode 26.4 final SDK.  2.2.5 (released 2026-07-10) wraps those
          # calls in #if compiler(>=6.3) / #available(iOS 26.4, *) guards.
          node -e "
            const fs = require('fs');
            const p = 'AllergyBusterApp/node_modules/react-native-iap/openiap-versions.json';
            const v = JSON.parse(fs.readFileSync(p));
            console.log('openiap apple before:', v.apple);
            v.apple = '2.2.5';
            fs.writeFileSync(p, JSON.stringify(v, null, 2));
            console.log('openiap apple after:', v.apple);
          "
          echo "=== openiap-versions.json after patch ==="
          cat AllergyBusterApp/node_modules/react-native-iap/openiap-versions.json
          # Delete Podfile.lock so pod install re-resolves rather than replaying
          # cached version pins (stale lock would keep openiap at 2.2.1).
          rm -f AllergyBusterApp/ios/Podfile.lock
          cd AllergyBusterApp/ios
          pod install --repo-update
          echo "=== Podfile.lock openiap pin ==="
          grep -A1 "openiap" Podfile.lock || echo "openiap not found in Podfile.lock"

      - name: Patch openiap Swift sources
        script: |
          BRIDGE=AllergyBusterApp/ios/Pods/openiap/packages/apple/Sources/Helpers/StoreKitTypesBridge.swift
          echo "=== openiap resolved version ==="
          grep -A1 "openiap" AllergyBusterApp/ios/Podfile.lock 2>/dev/null | head -5 || echo "(no Podfile.lock)"
          echo "=== bridge line count ==="
          wc -l "$BRIDGE"
          echo "=== static func names in bridge (unpatched) ==="
          grep -n "static func " "$BRIDGE" | grep -v "// XCODE26"
          echo "=== running patch ==="
          python3 scripts/patch-openiap.py
          echo "=== lines 628-650 after patch (around error 641) ==="
          sed -n '628,650p' "$BRIDGE"
          echo "=== lines 682-705 after patch (around error 690) ==="
          sed -n '682,705p' "$BRIDGE"
          echo "=== lines 880-930 after patch (around errors 888/915/921) ==="
          sed -n '880,930p' "$BRIDGE"
          echo "=== lines 1055-1075 after patch (around error 1068) ==="
          sed -n '1055,1075p' "$BRIDGE"

      - name: Set up keychain
        script: |
          keychain initialize

      - name: Fetch signing files from Apple
        script: |
          app-store-connect fetch-signing-files com.allergybusterapp \
            --type IOS_APP_STORE \
            --certificate-key "$CERTIFICATE_PRIVATE_KEY" \
            --create

      - name: Add certificates to keychain
        script: |
          keychain add-certificates

      - name: Build and export IPA
        script: |
          PROFILE=$(ls ~/Library/MobileDevice/Provisioning\ Profiles/*.mobileprovision | head -1)
          UUID=$(security cms -D -i "$PROFILE" | plutil -extract UUID raw -)
          TEAM=$(security cms -D -i "$PROFILE" | plutil -extract TeamIdentifier.0 raw -)

          cat > /tmp/ExportOptions.plist << EOF
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
            <key>method</key>
            <string>app-store-connect</string>
            <key>teamID</key>
            <string>$TEAM</string>
            <key>signingStyle</key>
            <string>manual</string>
            <key>provisioningProfiles</key>
            <dict>
              <key>com.allergybusterapp</key>
              <string>$UUID</string>
            </dict>
          </dict>
          </plist>
          EOF

          mkdir -p /tmp/xcodebuild_logs

          xcodebuild \
            -workspace AllergyBusterApp/ios/AllergyBusterApp.xcworkspace \
            -scheme AllergyBusterApp \
            -configuration Release \
            -archivePath $CM_BUILD_DIR/AllergyBusterApp.xcarchive \
            archive \
            CODE_SIGN_STYLE=Manual \
            PROVISIONING_PROFILE="$UUID" \
            DEVELOPMENT_TEAM="$TEAM" \
            CODE_SIGN_IDENTITY="iPhone Distribution" \
            2>&1 | tee /tmp/xcodebuild_logs/archive.log

          echo "=== SWIFT/COMPILE ERRORS ==="
          grep -E "error:|swift:|SwiftCompile" /tmp/xcodebuild_logs/archive.log | grep -v "^note:" | head -80 || true
          echo "=== END ERRORS ==="

          xcodebuild \
            -exportArchive \
            -archivePath $CM_BUILD_DIR/AllergyBusterApp.xcarchive \
            -exportPath $CM_BUILD_DIR/ipa \
            -exportOptionsPlist /tmp/ExportOptions.plist

      - name: Clean up keychain
        script: |
          keychain delete

    artifacts:
      - $CM_BUILD_DIR/ipa/*.ipa
      - /tmp/xcodebuild_logs/*.log
      - AllergyBusterApp/ios/Podfile.lock

    publishing:
      app_store_connect:
        api_key: $APP_STORE_CONNECT_PRIVATE_KEY
        key_id: $APP_STORE_CONNECT_KEY_IDENTIFIER
        issuer_id: $APP_STORE_CONNECT_ISSUER_ID
        submit_to_testflight: true
